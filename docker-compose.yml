version: '3.8'
services:
  web:
    build: ./src
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"

  db:
    build: ./database
    ports:
      - "5433:5432"